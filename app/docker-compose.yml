version: '3'
services:
  webapp:
    container_name: web-app-container
    image: 816002188/project-app
    ports:
     - 80:5000
    environment:
      DB_USER: stairs1
      DB_NAME: stairsdb
      DB_PASSWORD: password123
      DB_HOST: <Replace this with the endpoint string>
      DB_PORT: 3306
  
